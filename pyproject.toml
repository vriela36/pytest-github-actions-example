[project]
name = "pytest-github-actions-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "coverage-badge~=1.1.0",
    "pytest==7.2.2",
    "pytest-cov==4.0.0",
    "ruff==0.0.260",
]
